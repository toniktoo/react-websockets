import { createAction } from 'redux-actions';

export const setIsPainting = createAction('SET_IS_PAINTING');
export const changeColorMouse = createAction('CHANGE_COLOR_MOUSE');
export const changeSizeMouse = createAction('CHANGE_SIZE_MOUSE');
